import Prismic from 'prismic-javascript';

// Update your-repo-name with the name of your repository.
const apiEndpoint = 'https://mrus.prismic.io/api/v2';
const Client = Prismic.client(apiEndpoint, { req });
